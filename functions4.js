// exercicio 4

function novoArray(numeros = []) {
    const retorno = []
    const primeiroNumero = numeros[0]/2
    const ultimoNumeoro = numeros[numeros.length-1] / 2
    retorno.push(ultimoNumeoro)
    retorno.push(primeiroNumero)
    return retorno
}





 
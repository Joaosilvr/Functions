// exercicio 2

function receberNome(nome) {
    console.log(`Olá ${nome}.`);
}
receberNome('João')
receberNome('Guilherme')
receberNome('Will') 

function calcularArea(altura,largura) {
    const area = altura * largura
    return area
}
calcularArea(2,3)